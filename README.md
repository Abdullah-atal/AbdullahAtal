# AbdullahAtal V14 — Deployment & PWA

V14 is the phone-first deployment package:
- installable PWA manifest
- service worker/offline shell
- Android home-screen installation support
- Vercel SPA routing config
- Netlify SPA routing config
- Supabase launch verification SQL
- final phone launch checklist

Migration order: schema.sql → V5 → V6 → V7 → V8 → V9 → V10 → V11 → V12 → V13.

Use `LAUNCH.md` for the production checklist.
